/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

const SmileIDReactNativeWrapper = require("./App")

module.exports = {
    SmileIDReactNativeWrapper
}
